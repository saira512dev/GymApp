// prettier.config.js (ESM-compatible)
import prettierPluginTailwindcss from "prettier-plugin-tailwindcss";

/** @type {import("prettier").Config} */
const config = {
  plugins: [prettierPluginTailwindcss],
};

export default config;
